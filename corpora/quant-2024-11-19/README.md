# QUANT historical pilot

This pilot targets `3an8rhdepsLCya22af7qDBKPbdomw8K4iCHXaA2Gpump` on Solana mainnet. The selected half-open slot range is `[302459600, 302461200)` in epoch 700: 1,588 produced slots and 12 skipped slots over 11 minutes 25 seconds.

`paper-model.json` pins demo execution assumptions separately from the historical evidence: 250 ms modeled submission latency, 10% maximum participation in each eligible tape trade, a 60-second look-ahead, 5% market-price protection, a 1% modeled protocol fee, and a 5,000-lamport network fee. These values are intentionally configurable scenario inputs; they are not claims about fees, latency, or available liquidity on the recorded chain.

The first produced slot is timestamped `2024-11-20T03:48:28Z` and the last `2024-11-20T03:59:53Z`, which is `2024-11-19 19:48:28–19:59:53` in America/Los_Angeles. Selection time was verified from Old Faithful's epoch-700 slot-to-blocktime index, SHA-256 `0c642a5eba7800f4941f5e5e90cf58ac21e20806c72ea85593ffc4b2b4fee3ac`.

`extract-plan.json` pins the source objects, revisions, hashes, exact byte range, and hard resource ceilings. The planned range is 2,379,382,552 bytes; with the 59-byte CAR header and 5,184,000-byte slot index, extraction transfers 2,384,566,611 bytes and reserves 2,385,615,187 workspace bytes. The 673,522,614,052-byte epoch CAR is never downloaded in full.

The large raw extract is internal and is not committed to Git. Two independent downloads passed `fervor-corpus verify` with raw SHA-256 `fd3b452cc000ba80cef1b71ec999d32fe2826764c9f631425b40a011f72cd579` and index SHA-256 `035e344ab21076a8ba59d1317ffa4c99e914bdfa0f43e6cfe6d5bf84b5b7ad1e`.

At commit `40d461c`, both downloads replay to 1,588 blocks, 2,723,843 scanned transactions, 13,722 mint-matched transactions, and 262 currently supported swaps. Four independent replay runs produced byte-identical artifacts:

- transactions: `478a4336d26f0778681830d92e89890175d39a07cbf1a4d6c933e2dd4da3706e`;
- swaps: `39f0bab13b0bab28bcd7f92fb6c4768a707bab2e7ab1314aadae5e6e25a7aa75`;
- domain-separated replay: `e6cf77a7791c004d3cb420c1ca0dda30ae3bb310dbd5347d2c76ec833cfc132e`.

The acceptance run used the second download on a 16-vCPU GCP VM. It completed in 37.27 seconds at 389% average CPU with 354,752 KiB peak RSS, compared with 14:02.95 before filtering archive scan noise at the source boundary.

At commit `c21e448`, replay schema v2 also decodes the exact November 2024 Pump self-CPI layout and reconstructs lifecycle state. The two independent extracts produced byte-identical directories containing:

- manifest: `f18166d205a58e042b2967521f53acf9fda7e458acc464e3437ff3069acf0d7f`;
- transactions: `478a4336d26f0778681830d92e89890175d39a07cbf1a4d6c933e2dd4da3706e`;
- swaps: `39f0bab13b0bab28bcd7f92fb6c4768a707bab2e7ab1314aadae5e6e25a7aa75`;
- Pump events: `9ec4b7440554e4cc1b4994009b6cb5d6b82f72dee2e8193fac80dda1302c3f2b`;
- Pump state: `15f54ac175b27e8270791645f4f6d018309cd4ec9db1d2ea4489f7fcfb518a03`;
- domain-separated replay: `c3f46e619ad4c4d37fc5f8338770b67812e7c33287df21d08076c4d9ebf9d238`.

The qualified lifecycle contains one create, 250 committed trades, one complete, and one legacy withdraw. Nine Pump event CPIs from failed transactions are intentionally excluded because their state never committed. Creation CPIs prove six decimals, an initial raw supply of `1000000000000000`, and revoked mint authority. The final state is migrated with zero real token reserves, a price of `0.000000410880168542` SOL, and an FDV of `410.880168542336548767` SOL.

The independent v2 runs completed in 37.93 and 38.02 seconds at 384% average CPU, with 352,400 and 354,640 KiB peak RSS.

At commit `9c5ed16`, replay schema v3 emits the same `fervor-supply-v1` evidence contract consumed by the live and TypeScript metric paths. The event is anchored to Pump creation at slot `302459624`, signature `4mhRTtkQZLF6CL7joHwWwWdaSLP38or5WfhVY6DtZC4vKG3je1sxrDUbNtr2gyTvMXHGTynEUPC6M1NpQF7mbS8d`, instruction index 3, event index 0. It proves raw supply `1000000000000000`, six decimals, the `pump-event-2024-11-v1` layout, finalized commitment, and exact Old Faithful provenance. No RPC or metadata provider supplies this value.

Both independent extracts again produced byte-identical directories:

- manifest: `e0257286de86cde1382e4bb396ff2a5913c15037e3966f31f92050ea579d13a3`;
- transactions: `478a4336d26f0778681830d92e89890175d39a07cbf1a4d6c933e2dd4da3706e`;
- swaps: `39f0bab13b0bab28bcd7f92fb6c4768a707bab2e7ab1314aadae5e6e25a7aa75`;
- Pump events: `9ec4b7440554e4cc1b4994009b6cb5d6b82f72dee2e8193fac80dda1302c3f2b`;
- Pump state: `15f54ac175b27e8270791645f4f6d018309cd4ec9db1d2ea4489f7fcfb518a03`;
- supply: `b6a84094d265ea6ec2e68b10ffa9e6ae4f36f9c03bac9f834b80fbff76269e42`;
- domain-separated replay: `545b3c5dffe645387ecebc55a5267c6464810d91ab9267b9c550a85b9b42cacd`.

The v3 runs completed in 38.77 and 38.44 seconds at 381% and 384% average CPU, with 353,132 and 352,632 KiB peak RSS. The verified raw extracts and each qualified replay revision are retained in separate private, versioned GCP object-storage prefixes together with timing and build evidence.

At commit `0e9814f`, replay schema v7 includes the shared `fervor-trade-v1` tape, the owned `fervor-sol-usd-v1` reference tape, and 250 post-trade `fervor-pump-curve-v1` points. Every curve point preserves exact real and virtual reserve integers. Virtual reserves determine marginal price but are not called liquidity; `fervor-pump-real-reserve-mark-v1` marks only the two real reserves at that price and explicitly labels the result estimated.

The two independent extracts produced byte-identical v7 directories:

- manifest: `99cb792662e1e9417f3c55608d54b648a05666d1ba5b5603dd81fb3590919dea`;
- normalized trades: `0868a6e0be01eba91d2c7e75ab64e3d9e5f48a062362c5d761c8326404da1d28`;
- Pump curve: `72fc76254250383f47c6c2cebd29cfca86ee9f62e8a0cff76130c5a719e9929c`;
- SOL/USD tape: `1d19c086d4685334d9e40cc071c1aaf9fb4efc93fcafa95002715bba9716a906`;
- domain-separated replay: `b09fae64903dfab5b818a0b05bc307e4d219336f5f95f38848653f71febd0b3d`.

The curve points have 250 unique source-event IDs and exactly one completion. The first point is slot `302459624` at `2024-11-20T03:48:37Z`, with price `0.000000030824790307` SOL, FDV `30.824790307548925365` SOL, and estimated real-reserve mark `24.372141192917055790` SOL. The terminal point is slot `302460024` at `2024-11-20T03:51:29Z`, with price `0.000000410880168542` SOL, FDV `410.880168542336548767` SOL, and estimated real-reserve mark `85.005359175000000000` SOL.

The v7 runs completed in 40.32 and 40.38 seconds with 354,436 and 353,280 KiB peak RSS. The compact manifest, curve tape, and timing evidence are retained at the private versioned GCS prefix `replays/quant-2024-11-19/v7/`; both full replay directories and both source extracts remain on the replay VM.

At commit `21382ed`, the shared TypeScript engines project the verified v7 tape without network, database, or wall-clock access. Independent projections from the two source extracts produced byte-identical output with:

- 5,516 decoded trades, of which 5,494 have eligible historical USD evidence and 22 remain explicitly unpriced;
- 250 Pump curve states, of which 228 have eligible historical USD evidence, and 597 OHLCV candles across all supported intervals;
- projection SHA-256 `8b0de39397738de6609f56f15637751db1ead7ba3f4e783f0ec1e364757f7038`;
- manifest SHA-256 `4b477826be268b04858ac646f5864c855e45d84126965f1ce7b94c8ce77ca07d`;
- market-state SHA-256 `8dc0645fd4cfcf6306d5218d6f465cef1c490703a649d29f663f62eab212ef2d`.

The final state is timestamped `2024-11-20T03:59:53Z`: native price `0.000011717442528638452` SOL, USD price `0.0027406250200343965`, fixed total supply `1,000,000,000`, and FDV `$2,740,625.0200343966`. Circulating market cap remains unavailable. Current liquidity is also unavailable after migration; the last supported curve estimate is preserved separately as `85.005359175000000000` SOL, or `$19,817.997363471935` using the contemporaneous tape.

The projection runs completed in 2.31 and 2.18 seconds with 365,640 and 338,188 KiB peak RSS. Exact compact outputs are committed under `golden/`. The canonical 8.2 MiB projection and both timing records are retained in the private versioned GCS prefix `metrics/quant-2024-11-19/v1/`.

At commit `3f85107`, `fervor-trade-v2` restores the archive's proven transaction index to every normalized trade. Replay schema v8 and all TypeScript consumers now share slot, transaction, instruction, and event ordering. This corrects equal-time close-price selection that previously fell back to an arbitrary identity order.

Both independent raw extracts produced byte-identical v8 directories in 40.96 seconds, with 355,344 and 354,276 KiB peak RSS:

- source manifest: `baa006f8a0a39e3fc7fbdc7967c3bf11be5a9e3a7a35e8cafe9a6e47d79e3600`;
- normalized trade tape: `de9ec9f7d65feb395f97b495781ec0d9a772b845f5ec390f9e73fe64aaf09800`;
- domain-separated replay: `804d60b575012dba56f8ff62ea5e77c35214201ceeec3999b6743069eb048702`.

Independent TypeScript projections were again byte-identical. They completed in 2.51 and 2.38 seconds with 373,348 and 374,948 KiB peak RSS:

- projection manifest file: `841dc68ceb9713561caf76091e742d6371828fd8ec64d18f2b44301a33add34b`;
- enriched trades: `7215950af408d21b0a47b86b4c161de0384bfc57e893398a0d78e8834bfeb5ba`;
- candles: `b8803f3115875dbe1d81e2cf559761b2bb036854b86693444dacd2f07dcf3c4e`;
- market state: `427692352d825e07eedad9b347711027958f37d92075b3f47b0c5dde2b643b93`;
- complete projection: `c14a97d75bb7193b2d65588ca9c2845767fb8f6585e1cc5b201dd1d0348cf9d8`.

The corrected final trade price is `0.00001598430820249391` SOL or `$0.003738614026109172`, giving FDV `$3,738,614.026109172`. Counts, USD coverage, rolling windows, curve metrics, and supported-liquidity semantics are unchanged.

At commit `66a441c`, the runtime durably published a cursor-2,500 checkpoint under the v8 source digest. The checkpoint payload digest is `921e3de915c2391f9fb3859a4d4aed55fd8690567a19104744ac7c4f8da0d8b8`; its canonical 200,924-byte file has SHA-256 `d66a3b400ff58872fbca650cfbc91de978b5cf50b258e4bd1862426c756bc245` and was published in 15.52 ms.

A fresh Node process read and fully validated that file, restored the verified tape in 25.48 ms, and replayed the tail to cursor 5,516. Its final checkpoint exactly matches an independent clean process at SHA-256 `c4cf96bf76e34047b5427e7a57cd1853f23c6559f5c7f79561b16d63e5976f05`; rolling state and both terminal prices match the corrected goldens. The durable checkpoint is retained under `checkpoints/quant-2024-11-19/v1/`. Machine-readable recovery evidence with SHA-256 `11230e8f4e5ffbf82b59cd385087ee19517440d480637925652659eff8abfb98` and the replay timing records are retained under `evidence/quant-2024-11-19/v8/`; the full v8 replay and metric v2 projection remain under `replays/quant-2024-11-19/v8/` and `metrics/quant-2024-11-19/v2/`.

At commit `87e133f`, cumulative monotonic pacing ran the complete 5,516-event tape at 1x, 20x, 100x, and maximum speed in separate processes. Excluding corpus load, elapsed times were 676.003 seconds, 33.802 seconds, 6.762 seconds, and 61.40 milliseconds. Every run ended at the same cursor, virtual timestamp, and checkpoint SHA-256 `c4cf96bf76e34047b5427e7a57cd1853f23c6559f5c7f79561b16d63e5976f05`. The machine-readable result has SHA-256 `24fc67ba46ab6a2338a04e09851bb6e86f787e59c68dccb89a49cc1a5b96a86c` under `evidence/quant-2024-11-19/replay-speed-v1/`.

At commit `debea8e`, the local store admits exactly one checkpoint per source, checkpoint contract, and cursor, then selects the highest verified cursor not beyond the target. Real SIGKILL injection on the VM's ext4 filesystem covered temporary-file sync, atomic link, unlink entry, and the interval after unlink but before directory sync. Pre-publication crashes left no target and selected the prior checkpoint; post-publication crashes left a fully valid target whether or not the temporary remained. A fresh process selected the surviving cursor-3,000 checkpoint and reached the same clean final SHA-256. The machine-readable case record has SHA-256 `17fa25555c1ab7bc9c20529cf07436d244b0f034a09189310e92ba2127d83087`; it and all syscall traces are retained under `evidence/quant-2024-11-19/checkpoint-crash-v1/`.

At commit `197c9ed`, `replay-lab` ran as a single stdin/stdout control process under an empty environment. Its compiled local dependency graph contains 18 offline replay, metric, schema, and clock modules and no application environment loader, database, Redis, live provider, wallet, transaction, KMS, or notification module. The GCP acceptance stepped and checkpointed cursor 1, sought to cursor 2,500 on epoch 2, paused a 1x run at cursor 2,521, completed at maximum speed, persisted the exact final checkpoint, sought back to cursor 2,500 on epoch 3, and stopped. A separate launch with `DATABASE_URL` present failed before corpus loading and reported only the variable name. The final canonical file has SHA-256 `c293e9cd6bb3ac8694dee00571d346c653d7ee9b86333ec13a998a92f4e289ef`; machine-readable runtime evidence has SHA-256 `7a98dd30b203f1a1f3d8ebc8bfa3bf5df1a015e0cd754327c6ae5ce88b7ddf1c` under `evidence/quant-2024-11-19/replay-runtime-v1/`.

At commit `b57e4ef`, the production backend image ran the same control process inside the replay-only Compose service on the GCP replay VM. Docker's effective configuration reported no network, a read-only root, every capability dropped, privilege escalation disabled, the corpus mounted read-only, and only the checkpoint directory mounted read-write. Direct probes returned `Network unreachable` for external IP egress and `Read-only file system` for writes to both the image root and corpus. The sandbox reproduced the cursor-2,500 and final checkpoint digests, and a fresh container restored cursor 5,516 from the retained store and emitted the same final digest. The compact [sandbox evidence](sandbox-evidence.json) has SHA-256 `915ca98e754f36953eb0863b659f53454b88980e6511ccbc1ab868fc82ec9346`; a retained copy lives under `evidence/quant-2024-11-19/replay-sandbox-v1/`.

Two detached instances of that image then started concurrently with distinct run IDs and writable checkpoint mounts. Sending SIGKILL to run A produced exit code 137, left its store empty, and did not interrupt run B. Run B remained healthy, handled SIGTERM with exit code 0, and durably published only its own cursor-zero checkpoint. The compact [concurrency evidence](concurrency-evidence.json) has SHA-256 `b4bfc9767c08ecee877f57e8e2429b06f328c86b9aa3c5ac1460ffe035b0a382`; a retained copy lives under `evidence/quant-2024-11-19/replay-concurrency-v1/`.

At commit `c217cf9`, the same sandbox recovered a pending paper order after container exit code 137 and filled it only from a later, opposite-side historical trade. The 0.01 SOL input produced `170375702940` gross token units and `168671945910` net units after the pinned 1% protocol fee; the separate 5,000-lamport network fee remained denominated in SOL. The market state, order, four immutable facts, raw-unit fees, and parent-linked session were recovered together in a fresh container. An explicit seek to cursor zero then atomically cleared paper and market state, and another cold start recovered the reset at a higher epoch. The run also exposed and fixed a real-corpus timestamp boundary: valid second-precision RFC3339 input is now normalized before it enters durable state. Compact [paper-runtime evidence](paper-runtime-evidence.json), SHA-256 `f8b0461f771cc7c900273209857c47ce710520e2e68e1ad01ba4f0ad71f0f793`, retains the exact model, image, source, fact, session, and file hashes; the VM retains all three session files.

At commit `ddd8be2`, `fervor-paper-portfolio-v1` derives its entire economic view from the validated fill facts in that atomic session. The historical buy yields `168671945910` open token units at `10000000` lamports of trade cost, a `-10005000`-lamport signed SOL flow after the separately attributed network fee, and complete in-window basis. A fresh container restored cursor 60 at epoch 2 and reproduced portfolio SHA-256 `5823629fa14a70435c6d51baf17ba5bde0750297cf7a3fa9ab8b8f01a77c1be2`. No portfolio table or second checkpoint exists. Compact [portfolio evidence](portfolio-evidence.json), SHA-256 `82611e40cb01a8a9690e1123cf70fbd96a694b9cba0ae776188dd2795d89c8ca`, retains the exact flows, fees, position, image, session, and cold-recovery hashes.

At commit `d35166e`, `fervor-replay-wallet-trade-v1` pages maker activity directly from the verified trade tape at the current replay cut. A real wallet bought at cursor 14 and sold at cursors 47 and 58; exact raw deltas, signatures, protocol, and canonical chain positions survived cursor paging and a cold container restart with identical activity keys. Coverage remains explicitly trade-only and limited to the selected window, so no starting balance, transfer history, or complete fee history is invented. No wallet worker, table, or checkpoint exists. Compact [wallet evidence](wallet-evidence.json), SHA-256 `6e37e119a5067401aba35f9860f4ef1d8b555ac302a5c43a17c071324a1fb457`, retains the exact page boundaries and activity identities.

At commit `5e48971`, the wallet view and paper broker share one exact FIFO basis authority. The same real wallet has `3726932913640` raw token units open at `201339752` lamports of observed cost after two fully covered sells, with `82716856` lamports of observed realized PnL and `87324579` lamports unrealized under the cursor-59 last-trade mark. A second container rebuilt the identical portfolio digest without wallet state. The contract still labels opening balances and transfers unknown; observed coverage is not complete history or tax basis. Compact [wallet portfolio evidence](wallet-portfolio-evidence.json), SHA-256 `99958703f631bbfc5becd7d525106c9dd6134eb58968d6b22ea42f13db4d26aa`, retains the exact coverage, flows, position, mark, and cold-recovery result.

At commit `32d9055`, a source-bound one-shot alert model projects an in-app inbox directly from the verified tape and the shared TypeScript metric/threshold authorities. At cursor 60, price crossed `$0.00001` at cursor 22 and rolling one-minute volume crossed `$5,000` at cursor 51. Market cap is reported unavailable at this cut instead of being fabricated. Both notifications carry virtual event time, confidence, provenance, epoch-fenced identities, and `external: false`; no database, Redis, provider, or second checkpoint was added. A cold container rebuilt the identical notification list at SHA-256 `8a8b9f0a31fd64cc14ddce1112e64fbcfaf478c57dfc064be289b2eed627f597`. Compact [alert evidence](alert-evidence.json), SHA-256 `2063d7678eae9e807ca5a65e6531926ca22fd5474d6b7db51e2061793ddbc0eb`, records both triggers, the unavailable definition, image identity, tests, and exact cold-recovery comparison.

At commit `5dcbe62`, the same source-bound model also watches tracked-wallet buys and sells. The real wallet's cursor-14 buy and cursor-47 sell became one-shot `fervor-replay-wallet-in-app-v1` records containing the exact existing wallet activity objects and raw deltas. They interleave canonically with the cursor-22 price and cursor-51 volume notifications; a later sell does not retrigger the one-shot watch. The prior metric-only model still normalizes to its original digest. A cold container rebuilt the entire mixed inbox at SHA-256 `b9743e2805b40834d4b6cd09c1dda30c5823622a68958f381596478455674cb0`. Compact [wallet-alert evidence](wallet-alert-evidence.json), SHA-256 `99f586998fe80a2c86f06904a73581a7f392ae4435575c1b0ef8069cfbacb6a2`, retains the exact wallet identities, events, image, tests, and cold comparison.

At commits `338da9e` and `de28bc8`, the replay owner exposes its snapshot and inbox over an owner/group-only Unix socket without gaining a network interface or importing the production API's database, Redis, JWT store, or provider graph. The GCP service requires a source/run-bound bearer hash, the dedicated replay URL, and `X-Fervor-Mode: historical_replay`; missing mode returned 409, missing auth 401, the wrong run 404, and valid snapshot/inbox requests 200. A competing container exited 1 without unlinking the healthy owner's socket. Graceful rotation removed the old socket, restored cursor 60 at epoch 3, and retained the same session ID while epoch-fencing notification keys. The running container has a read-only root, no capabilities, no-new-privileges, and `network_mode: none`; direct egress returned `ENETUNREACH`. Compact [replay API evidence](replay-api-evidence.json), SHA-256 `2956e2c33f5acb98e356035dad4ecd93afd36b5c385bd5d002db1679e0716051`, retains the API responses, mixed inbox order, socket/auth permissions, collision, recovery, sandbox, image, and test evidence.

At commit `34b5c8e`, the same authenticated read boundary adds bounded `fervor-replay-delta-v1` pages over the immutable verified tape. GCP restored the real run at epoch 3/cursor 60, returned contiguous cursors 55–57 through that exact cut, returned an empty successful page when caught up, and returned typed 409 resync responses for both an earlier epoch and a cursor ahead of the cut. The full non-database suite passed in a separate networkless, read-only container; the original API owner remained running. Compact [delta evidence](replay-delta-evidence.json), SHA-256 `eab09a8fe5985652b21e836a94a4fd88e1ce4ee2eac549c9c17f461cb134ab47`, records the source commit, real event identities, responses, sandbox, harness corrections, and zero external effects without recording the bearer token.

At commit `c1d4929`, exact-cut reads expose bounded paper orders/facts with the derived portfolio, a selected wallet's bounded trades with observed-basis portfolio, and an optionally cut-bound inbox. Paper reads also bind the fact count, which fences order changes made without advancing replay time. GCP paged the restored fill history, projected both portfolios, placed a replay-only order at the unchanged cursor, rejected the old paper revision, accepted the new revision, and persisted session sequence 2. The checkpoint bundle is retained in the versioned GCS bucket; all API owners remained networkless and running. Compact [product evidence](replay-product-evidence.json), SHA-256 `9df64043a3c738e5947b2533f5181523a8a988eb0a7825eef3b17b00fef18eb5`, records these results without credentials.

At commit `00d2799`, external replay delivery remains impossible without a short-lived permit that names the exact corpus, run, epoch, alert model, notification key, channel, and replay-only destination. Authority is reloaded before each attempt; local fake-provider tests cover explicit `Retry-After`, bounded retry-safe failures, unknown effects, seek fencing, and destination removal. The GCP acceptance bound the real cursor-14 wallet alert from the epoch-3 inbox to a loopback-only Telegram fixture. It was accepted once with the label `FERVOR REPLAY - HISTORICAL DATA`; the raw destination was absent from retained evidence, and no live credential, provider, external network, database, Redis, or transaction path was available. The no-network/read-only/capability-free test image passed 58 backend files and 370 tests, with the 14 database-backed files skipped as requested. Compact [replay-send evidence](replay-send-evidence.json), SHA-256 `b6fb02c1b01bff8e4da7c14eeadf11e72d3ad24e59a396f01c0121a88b3e6d66`, records image, permit, message, test, and failed-harness identities; its sanitized acceptance payload is retained in GCS at SHA-256 `668e0c52b9ff4b6c989947e30c26ce0956e2b7c272857e64238cbd249f75651e`.

At commit `0e2d95a`, the authenticated replay boundary accepts strict, replay-only paper place and cancel commands at an exact epoch/cursor/fact revision. The real corpus acceptance placed one limit order, converged an original-request retry without another fact, rejected conflicting reuse of the order ID, fenced a different stale command, cancelled once, and converged the cancellation retry. Replay time stayed frozen at cursor 60; no provider call or transaction path was present. A new process restored the cancelled order at fact 6 and epoch 4. The deterministic session archive and sanitized HTTP responses are retained in the versioned GCS bucket. Compact [action evidence](replay-action-evidence.json), SHA-256 `5d8bcbc87083f38df4d134133feebd5eb805761034e869bbb15ed5f4bcd015dc`, records the contracts, revisions, recovery chain, image, sandbox, tests, and zero external effects without credentials.

At commit `181ad02`, the same authenticated boundary exposes play, pause, step, and seek without creating another replay owner. The runtime now serializes HTTP and stdin mutations, distinguishes playback from non-atomic transitions, and suppresses reads during seek. On the real corpus, 1× play advanced from cursor 60 to 75 before a deliberately stale same-epoch pause stopped it; the retry was a no-op, one step advanced exactly to 76, and the stale step was fenced. A no-op seek preserved all six paper facts, while a real backward seek advanced the epoch and deliberately cleared them. Maximum-speed playback reached all 5,516 events, then a second seek returned to cursor 60. A fresh process restored the resulting paused state at epoch 7. The deterministic session archive and sanitized HTTP responses are retained in GCS. Compact [control evidence](replay-control-evidence.json), SHA-256 `2b5ca074266eae768b91c8ebe908137560f59ded73e2d7a7102237eefb59d2d8`, records the contracts, cuts, image, recovery, tests, sandbox, and zero external effects.
